import { Request, Response } from 'express';

class NursesController {
  public async create(
    request: Request,
    response: Response,
  ): Promise<Response> {}
}

export default NursesController;
